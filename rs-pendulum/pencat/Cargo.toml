[package]
name = "pencat"
version = "0.1.0"
edition = "2024"

[dependencies]
pendulum-lib = { path = ".." }
uom = { version = "0.38.0", default-features = false, features = ["autoconvert", "f64", "si", "serde"] }
